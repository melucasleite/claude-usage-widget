# ---------------------------------------------------------------------------
# Credentials — never, ever commit these.
# The app reads the Claude OAuth token from the macOS Keychain (or from
# ~/.claude/.credentials.json) at runtime. Nothing secret should ever live in
# this repository, but these patterns are here as a hard backstop.
# ---------------------------------------------------------------------------
.credentials.json
*credentials*.json
*.pem
*.key
*.p12
*.mobileprovision
.env
.env.*
!.env.example
secrets/
**/ANTHROPIC_API_KEY*

# Generated by XcodeGen from project.yml — never edit or commit these.
# `xcodegen generate` recreates both.
ClaudeUsageWidget.xcodeproj/
Resources/Generated-Info.plist

# Build artifacts
.build/
.swiftpm/
DerivedData/
*.xcworkspace/xcuserdata/
xcuserdata/
dist/
*.app
*.dSYM
*.o
*.swiftmodule

# macOS
.DS_Store
