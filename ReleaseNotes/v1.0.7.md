### Fixed
- Usage data works again: the widget reads Claude Code's own sign-in, which a `claude setup-token` token cannot replace.
- Rings no longer clip when the frosted background is turned off.

### Added
- The widget refreshes Claude Code's credential itself when it goes stale.
